# Leave blank to run the engine in mock mode (canned but realistic output).
ANTHROPIC_API_KEY=

# Which model to use when ANTHROPIC_API_KEY is set.
CAMPAIGNCRASH_MODEL=claude-sonnet-4-6

# Comma-separated list of origins allowed to call the API.
CORS_ORIGINS=http://localhost:5173
